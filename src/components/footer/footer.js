import Vue from 'vue'
// import http from '../../utils/HttpClient'
import router from '../../router/index'

const state = {
	footer:true
}

const mutations = {
	
}

const actions = {
	
}

export default {
	state,
	mutations,
	actions
}